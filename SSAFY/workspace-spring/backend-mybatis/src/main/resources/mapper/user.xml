<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<insert id="insert" parameterType="backend.server.salendar.dto.User">
       insert into user (usr_email,usr_password,usr_nick,usr_following) values (#{usr_email},#{usr_password},#{usr_nick},#{usr_following})
	</insert>  		

	 <update id="update" parameterType="backend.server.salendar.dto.User">
	    update user set usr_password=#{usr_password},usr_nick=#{usr_nick},usr_following=#{usr_following} where usr_email=#{usr_email}
	 </update> 
	 
	 <delete id="delete" parameterType="String">
	    delete from user where usr_email=#{usr_email}
	 </delete>

	<select id="select"  parameterType="backend.server.salendar.dto.User"  resultType="backend.server.salendar.dto.User">
	    select usr_email,usr_password,usr_nick,usr_following from member where usr_email=#{usr_email}
	 </select>

     <select id="login"  parameterType="backend.server.salendar.dto.User"  resultType="int">
	    select count(*) from user where usr_email=#{usr_email} and usr_password=#{usr_password}
	 </select>
	 
	 <select id="selectAll" resultType="backend.server.salendar.dto.User">
	    select usr_seq,usr_email,usr_password,usr_nick,usr_following from user
	 </select>

	
</mapper>







